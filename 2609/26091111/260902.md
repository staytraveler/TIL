# 260902 TIL 김용현

### 깃 & 깃허브

# 1. 오늘 배운 것(청년취업사관학교 : 권은지 강사님)

- `git config`로 사용자 이름과 이메일 설정
- 폴더에서 Git Bash를 열어 직접 Git 명령어 실행
- `git init`으로 저장소 초기화
- `git add`로 변경사항 스테이징
- `git commit`으로 변경사항 커밋
- `git log`로 커밋 히스토리 확인
- `git remote add origin`으로 원격 저장소 연결
- `git remote -v`로 원격 저장소 연결 상태 확인
- `git push -u origin main`으로 업스트림 설정과 함께 첫 푸시 진행
- `git branch`로 새 브랜치 생성
- `git switch`로 브랜치 이동
- `git merge`로 브랜치 병합
- 포크해온 원격 저장소를 `git clone`으로 로컬에 복제


# 2. 명령어 정리

#### 사용자 정보 설정 (config)

```bash
git config --global user.name "이름"
git config --global user.email "이메일"
```

- 커밋할 때 "누가 작업했는지" 기록되는 이름과 이메일을 설정한다.
- `--global` 옵션을 붙이면 내 컴퓨터 전체에서 공통으로 적용된다 (한 번만 설정하면 됨).
- 이 정보는 GitHub 로그인 계정과는 별개이며, 단순히 커밋 기록에 남는 "작성자 이름표" 역할이다.

#### Git 초기화

```bash
git init
```

- 현재 폴더를 Git이 관리하는 저장소로 초기화한다.

#### 변경사항 스테이징 (add)

```bash
git add [파일명]
git add .
```

- `git add [파일명]` : 특정 파일만 스테이징
- `git add .` : 변경된 모든 파일 한번에 스테이징

#### 커밋 (commit)

```bash
git commit -m "커밋 메시지"
```

- 스테이징된 내용을 하나의 기록(커밋)으로 저장한다.

#### 커밋 히스토리 확인 (log)

```bash
git log
git log --oneline
```

- `git log` : 커밋 히스토리를 작성자, 날짜, 커밋 메시지, 해시값까지 상세하게 보여준다.
- `git log --oneline` : 각 커밋을 한 줄로 축약해서 보여준다 (해시 앞 7자리 + 메시지). 히스토리를 한눈에 훑어볼 때 유용하다.

#### 원격 저장소 연결 (remote)

```bash
git remote add origin [저장소 URL]
```

- 로컬 저장소와 원격 저장소(GitHub 등)를 연결한다.
- `origin`은 원격 저장소를 가리키는 기본 별명이다.

#### 원격 저장소 연결 확인 (remote -v)

```bash
git remote -v
```

- 현재 연결된 원격 저장소의 이름과 URL을 확인한다.
- 정상 연결되었다면 fetch용, push용 두 줄로 URL이 출력된다.

#### 원격 저장소로 첫 전송 (push)

```bash
git push -u origin main
```

- 로컬 커밋을 원격 저장소로 업로드한다.
- `-u` (upstream) 옵션으로 로컬 브랜치와 원격 브랜치를 연결해두면, 이후부터는 `git push`만 입력해도 어디로 보낼지 자동으로 기억한다.

#### 브랜치 생성

```bash
git branch [브랜치명]
```

- 새로운 브랜치를 생성한다.
- main에 영향을 주지 않고 독립적으로 작업할 수 있다.

#### 브랜치 이동 (switch)

```bash
git switch [브랜치명]
git switch -c [브랜치명]   # 생성 + 이동을 한번에
```

- 지정한 브랜치로 이동한다.
- 기존 `git checkout`보다 최근에 나온 명령어로, 브랜치 전환 기능만 전담한다.

#### 브랜치 병합 (merge)

```bash
git switch main
git merge [브랜치명]
```

- main 브랜치로 이동한 뒤, 작업하던 브랜치를 병합한다.
- 병합 시 충돌(conflict)이 발생하면 해당 파일을 직접 수정한 후 다시 커밋해야 한다.

#### 원격 저장소 복제 (clone)

```bash
git clone [저장소 URL]
```

- 원격 저장소(예: 포크해온 레포지토리)를 로컬 컴퓨터로 통째로 복제한다.
- `.git` 폴더까지 포함된 전체 히스토리가 그대로 딸려온다.


# 3. 느낀 점

- 자주 쓰이는 명령어들이라 확실히 익혀둬야겠다.
- push 하기 전에 main 브랜치를 먼저 pull 받아서 최신 상태로 맞춰두면, 협업 시 충돌을 미리 방지할 수 있어서 좋은 습관인 것 같다.


# 4. 파생 학습 내용 (개인 공부)

수업 내용은 아니지만, 커밋 메시지를 고치고 싶어 궁금해져서 추가로 찾아본 내용들.

#### Vim 기본 조작

```
i          # Insert 모드 진입 (타이핑 가능)
Esc        # Normal 모드로 복귀
:wq        # 저장하고 종료
:q!        # 저장 안 하고 그냥 종료
```

- Git이 커밋 메시지 등을 작성시킬 때 기본 에디터로 Vim을 띄운다.
- 빈 메시지 상태로 `:wq` 하면 "empty commit message" 에러로 중단된다.

#### 최근 커밋 메시지만 수정 (amend)

```bash
git commit --amend -m "새 메시지"
```

- 바로 직전 커밋 하나만 메시지/내용을 고칠 때 사용.
- 이미 push한 커밋이면 이후 강제 push 필요.

#### 여러 개(또는 과거) 커밋 메시지 수정 (interactive rebase)

```bash
git rebase -i HEAD~N     # 최근 N개 커밋 대상
git rebase -i --root     # 최초 커밋(root)까지 전부 대상
```

- 목록에서 고치고 싶은 커밋의 `pick`을 `reword`로 변경 → 저장.
- `reword` 표시한 커밋마다 순서대로(위→아래, 오래된 것부터) 메시지 입력 창이 뜸.
- 수정하고 싶은 내용으로 적고 저장하면 됨.
- 작업 중(unstaged) 변경사항이 있으면 rebase 시작 자체가 안 됨 → 먼저 `commit` 또는 `stash` 필요.

#### rebase 중단하고 원래 상태로 되돌리기

```bash
git rebase --abort
```

- 진행 중인 rebase를 전부 취소하고, 시작하기 전 상태로 복구.

#### rebase 이어가기

```bash
git rebase --continue
```

- reword/edit 등으로 멈춘 지점을 처리한 뒤, 다음 커밋으로 진행.

#### 강제로 원격을 덮어쓰기 (force push)

```bash
git push --force-with-lease origin main   # 비교적 안전한 강제 push
git push --force origin main               # 무조건 덮어쓰기
```

- rebase 등으로 커밋 해시가 바뀌면 일반 push는 거부되므로 강제 push가 필요함.
- `--force-with-lease`는 원격에 내가 모르는 변경사항이 있으면 막아주는 안전장치가 있지만, 그래서 오히려 거부당할 수도 있음 → 그럴 땐 로컬 상태가 확실히 맞다는 전제하에 `--force`로 진행.
- 혼자 쓰는 개인 저장소에서는 비교적 안전, 협업 저장소에서는 신중하게 사용해야 함.