# 260904 TIL 김용현

## 완성한 과제
- [코드 보러가기](https://github.com/staytraveler/learning-log/blob/main/2026-bootcamp-sesac/css-practice.html)

## 학습내용 CSS & JavaScript (청년취업사관학교 : 권은지 강사님)

### CSS

**flex** — 자식 요소를 가로/세로로 배치
​```css
.container {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
}
​```

**grid** — 격자(행/열) 형태로 배치
​```css
.container {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
}
​```

**position** — 요소의 위치 지정 방식
​```css
.box {
    position: relative;
}
.badge {
    position: absolute;
    top: 0;
    right: 0;
}
​```

**:hover, :active** — 마우스 상태에 따른 스타일
​```css
.button:hover {
    background-color: skyblue;
}
.button:active {
    transform: translateY(2px);
}
​```

**transition** — 상태 변화가 부드럽게 이어지도록
​```css
.button {
    transition: all 0.2s ease;
}
​```

#### 기타 CSS
- `box-sizing: border-box`: padding, border가 있어도 지정한 width가 그대로 유지됨
- 반응형 대응
  - 이미지에 width/height를 고정하면 화면이 좁아져도 유지되어 튀어나감 → `max-width: 100%; height: auto;`로 대응
  - `<pre>`, `table`처럼 줄바꿈/고정폭 성질이 있는 태그는 좁은 화면에서 깨지기 쉬움 → `white-space: pre-wrap`, `overflow-x: auto`로 대응
- `:root`에 CSS 변수(`--변수명`)를 선언하면 색상 등을 한 곳에서 관리 가능
- 선택자 우선순위: 태그 < 클래스 < 아이디 < 인라인 스타일 < `!important`

### JavaScript

#### JS의 역사 (배경 지식)
- 예전엔 화면에서 뭘 하나 바꾸려면 페이지 전체를 새로고침해야 했음
- 1999년, AJAX 등장 → 필요한 부분만 서버와 통신해서 바꿔치기 가능해짐
- 이후 jQuery 등장, AJAX/DOM 조작을 훨씬 쉽게 다룰 수 있게 해줌
- V8 엔진 등장 → 자바스크립트 실행 속도가 크게 빨라짐
- 2009년, Node.js 등장 → 자바스크립트를 브라우저 밖(로컬, 서버)에서도 실행 가능해짐
  - 이 덕분에 프론트엔드뿐 아니라 백엔드까지 다룰 수 있는 언어로 확장됨

#### 변수 선언 키워드
- `var`: 재선언 가능(위험) → 요즘 거의 안 씀
- `let`: 재할당 가능, 재선언 불가, 블록 스코프
- `const`: 재할당/재선언 둘 다 불가 (상수) → 기본적으로 이거 먼저 쓰고 필요시 `let`

#### 데이터 타입
JS는 변수에 들어있는 값에 따라 타입이 결정됨 (동적 타입)
- `number`, `string`, `boolean`, `null`, `undefined` 등이 있음
- `undefined`: 값이 할당되지 않았을 때 자동으로 들어가는 값

​```javascript
console.log(typeof 10);
console.log(`합계: ${1 + 2}`);
​```

#### 암묵적 타입 변환
자바스크립트 엔진이 문맥에 맞게 알아서 타입을 바꿔주는 것

​```javascript
console.log(10 > '5');
console.log(+'');
​```

**Truthy / Falsy** — 불리언으로 평가될 때의 기준
​```javascript
// Falsy: false, 0, '', null, undefined, NaN / 이 외엔 전부 Truthy
if (10 > 5) console.log('참 입니다.');
if ('') console.log('Falsy');
if (0) console.log('ㅋ');
​```

#### 명시적 타입 변환
개발자가 의도적으로 타입을 바꾸는 방법

​```javascript
// 문자열로 변환
String(123);
(123).toString();  // null/undefined는 에러남

// 숫자로 변환
Number('123');
parseInt('123.45');    // 정수만
parseFloat('123.45');  // 실수까지

// 불리언으로 변환
Boolean(1);
!!1;  // !를 두 번 써서 변환
​```