# 260903 TIL 김용현

## HTML & CSS

### 완성한 과제
- [코드 보러가기](https://github.com/staytraveler/learning-log/blob/main/2026-bootcamp-sesac/html-practice.html)

### 학습내용 (청년취업사관학교 : 권은지 강사님)

#### 잡다한 스킬
- vscode에서 `!` + 엔터를 눌러서 HTML 뼈대를 자동완성
- `Ctrl + /`로 주석 기호 자동완성
- 태그 * 숫자하면 여러개 생성

#### text와 list
- h1~6 : 제목 글자 크기
- hr : 구분선, br : 줄바꿈
- p 태그는 하나의 문단, 문단 내에서 줄 바꿈을 해도 일렬로 나옴
- pre 태그는 p태그와 달리 쓴 모양을 그대로 보여줌
- b : 굵은글씨(스타일만) / strong : 굵은글씨(의미까지 담는 태그)
- i : 기울인 글씨(스타일만) / em : 기울인 글씨(의미까지 담는 태그)
- mark : 형광펜으로 칠한 것처럼
- del : 삭제된 내용 표시(취소선)
- ul : 순서없는 리스트, ol : 순서있는 리스트
- li : 리스트 항목

#### table과 media
- table : 표 전체를 감쌈
- caption : 표의 제목/설명 (table 태그 바로 다음에 와야 함)
- thead : 표의 제목 행들을 묶는 영역
- tr : 표의 한 '행'
- th : 표의 제목 셀, 자동으로 굵게 표시됨
- tbody : 테이블 내용 영역
- td : 표의 각 셀
- 속성 `rowspan="2"` 세로로 2칸 병합, `colspan="3"` 가로로 3칸 병합

```html
<img src="이미지 주소" alt="이미지 설명(사진 안 나올 시 대신 보임)" width="" height="">

<figure>
    <img src="...">
    <figcaption>이미지에 대한 설명</figcaption>
</figure>
```

- video, audio 태그 + `controls` (재생 컨트롤러 표시)

#### 링크와 폼

```html
<a href="주소" target="_blank">링크</a>
```
- `href`에는 외부 주소, 페이지 내 위치(`#id`), 다른 파일 경로 등을 넣을 수 있음
- `target="_blank"` : 새 창으로 열기

```html
<form>
    <fieldset>
        <legend>방명록</legend>
        <label for="user-id">아이디:</label>
        <input id="user-id" name="userId" type="text" placeholder="아이디를 입력하세요" required>
    </fieldset>
</form>
```
- `form` : 사용자가 입력한 데이터를 서버로 전송
- `fieldset` : 폼 영역을 테두리로 묶음
- `legend` : fieldset의 제목, 좌측 상단에 표시됨
- `label` + `for` : input의 `id`와 연결해서 사용성 향상
- `required` : 반드시 채워야 폼 제출 가능

**input의 다양한 type**
- text, password, number, radio, checkbox 등

```html
<select name="track" id="user-track">
    <option value="">희망 분야 선택</option>
    <option value="frontend">프론트엔드</option>
    <option value="backend">백엔드</option>
    <option value="ai">AI</option>
</select>
```
- select + option : 드롭다운으로 하나의 선택지 고르기

- radio, checkbox는 `name=""`으로 그룹을 묶음
    - radio : 여러 보기 중 **하나만** 선택
    - checkbox : **여러 개** 선택 가능
    - 보통 `value` 값을 함께 지정

- textarea : 여러 줄 입력, `rows` 등으로 크기 조절 가능
- button : `type="submit"`(폼 제출) / `type="reset"`(초기화)

#### div와 span / 시맨틱 — 영역을 나누는 태그들
- div : 한 줄을 통째로 차지 (블록 요소)
- span : 콘텐츠 크기만큼만 차지 (인라인 요소)
- 영역을 나눌 땐 시맨틱 방식을 선호하는 게 좋음

| 태그 | 역할 |
|---|---|
| header | 주로 상단, 로고/제목/메뉴 영역 |
| main | 메인(본문) 영역 |
| section | main 안에서 구역 나눌 때, id 지정해두면 좋음 |
| footer | 하단 영역 |

### CSS 선택자의 종류

**CSS 적용 방법 3가지**
1. 인라인 방법 : 태그에 직접 `style="..."`
2. 내부 스타일 시트 : `<head>` 안에 `<style>` 태그로 작성
3. 외부 스타일 시트 : 별도 `.css` 파일 생성 후 연결

**선택자 우선순위**
같은 요소에 여러 스타일이 동시 적용될 때:

```
태그 < 클래스 < 아이디 < 인라인 스타일 < !important
```
(오른쪽으로 갈수록 우선순위 높음, `!important`는 다른 우선순위 다 무시하고 무조건 이김)

**선택자 규칙들**

```css
/* 태그 선택자 */
h2 {
    color: red;
}

/* 클래스 선택자 : .으로 시작, 그룹으로 묶고 싶을 때 주로 씀 */
.highlight-text {
    background-color: beige;
}

/* 아이디 선택자 : #으로 시작, 단 하나뿐인 요소에 사용 */
#blue-text {
    color: blue;
}

/* 후손 선택자 : 띄어쓰기로 구분, 특정 요소 '안에 있는' 모든 후손 선택 */
#lecture-list li {
    font-weight: bold;
}

/* 자손 선택자 : > 기호로 구분, 특정 요소의 '바로 아래 자식'만 선택 */
body > p {
    font-size: 30px;
}

/* 속성 선택자 : 특정 속성이나 속성값을 가진 요소 선택 */
input[type="text"] {
    border: 2px solid orange;
}

/* 인접 형제 선택자 : 바로 뒤에 이어지는 형제 요소 선택 */
input[type="radio"]:checked + label {
    font-weight: bold;
    color: blueviolet;
}
```

**구글 폰트**
- 구글 폰트 사이트에서 원하는 폰트의 관련 코드를 `<head>` 영역에 넣으면 적용 가능
